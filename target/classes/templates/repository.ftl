package cn.zbxx.dao.${d.modelName};

import org.springframework.data.jpa.repository.JpaRepository;

import cn.zbxx.entity.${d.name};

public interface ${d.name}Repository extends JpaRepository<${d.name}, Integer> {

}
